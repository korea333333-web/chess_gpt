export const fixedCameraView = {
  position: [0, 8.1, 8.65] as [number, number, number],
  fov: 44,
  target: [0, 0, 0] as [number, number, number]
};

export const fixedCameraControls = {
  enableRotate: false,
  enableZoom: false,
  enablePan: false
};
