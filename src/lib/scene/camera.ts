export const fixedCameraView = {
  position: [0, 7.2, 7.6] as [number, number, number],
  fov: 42,
  target: [0, 0, 0] as [number, number, number]
};

export const fixedCameraControls = {
  enableRotate: false,
  enableZoom: false,
  enablePan: false
};
